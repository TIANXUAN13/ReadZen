version: '3.8'

services:
  app:
    build: .
    ports:
      - "5000:5000"
    environment:
      - ADMIN_PASSWORD=your_secure_password_here
      - HOST=0.0.0.0
      - PORT=5000
    volumes:
      - ./data.db:/app/data.db
    restart: unless-stopped